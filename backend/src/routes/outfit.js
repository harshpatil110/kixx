const express = require('express');
const router = express.Router();

/**
 * Note: The previous /analyze route using Gemini has been deprecated 
 * and moved to /api/ai/analyze-outfit handled by aiController.js
 * to support the pivot to NVIDIA's Qwen model.
 */

module.exports = router;
